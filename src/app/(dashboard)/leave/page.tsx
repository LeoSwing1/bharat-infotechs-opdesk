"use client";
import {useEffect,useState} from "react";
export default function LeavePage(){const[rows,setRows]=useState<any[]>([]);const[busy,setBusy]=useState(false);async function load(){const r=await fetch('/api/leave');const j=await r.json();setRows(j.requests??[])}useEffect(()=>{load()},[]);async function act(id:string,status:string){setBusy(true);await fetch('/api/leave',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status})});setBusy(false);load()}return <div><h1 className="text-2xl font-black">Leave</h1><p className="mt-1 text-sm text-gray-500">Requests, approvals and workforce availability.</p><div className="mt-6 overflow-hidden rounded-xl border bg-white"><table className="w-full text-sm"><thead className="bg-gray-50 text-left"><tr><th className="p-3">Employee</th><th className="p-3">Type</th><th className="p-3">Dates</th><th className="p-3">Days</th><th className="p-3">Status</th><th className="p-3"/></tr></thead><tbody>{rows.map(x=><tr key={x.request.id} className="border-t"><td className="p-3 font-semibold">{x.userName}</td><td className="p-3">{x.request.type}</td><td className="p-3">{x.request.startDate} → {x.request.endDate}</td><td className="p-3">{x.request.days}</td><td className="p-3">{x.request.status}</td><td className="p-3">{x.request.status==='PENDING'&&<span className="flex gap-2"><button disabled={busy} onClick={()=>act(x.request.id,'APPROVED')} className="rounded-lg bg-gray-900 px-3 py-1.5 text-white">Approve</button><button disabled={busy} onClick={()=>act(x.request.id,'REJECTED')} className="rounded-lg border px-3 py-1.5">Reject</button></span>}</td></tr>)}</tbody></table>{!rows.length&&<div className="p-8 text-center text-gray-500">No leave requests yet.</div>}</div></div>}
