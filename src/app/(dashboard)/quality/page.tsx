"use client";
import {useEffect,useState} from "react";
export default function QualityPage(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/quality').then(r=>r.json()).then(j=>setRows(j.scores??[]))},[]);return <div><h1 className="text-2xl font-black">Quality & Performance</h1><p className="mt-1 text-sm text-gray-500">Transparent scoring history across the workforce.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{rows.map(x=><div key={x.score.id} className="rounded-xl border bg-white p-5"><div className="flex justify-between"><div className="font-semibold">{x.userName}</div><div className="text-xl font-black">{x.score.score}</div></div><div className="mt-2 text-xs text-gray-500">{x.score.category} · {x.score.period}</div>{x.score.comment&&<p className="mt-3 text-sm text-gray-600">{x.score.comment}</p>}</div>)}</div>{!rows.length&&<div className="mt-6 rounded-xl border bg-white p-8 text-center text-gray-500">No quality scores recorded.</div>}</div>}
